# 拜访后收敛与落地方案

客户：`{customer_name}`  
拜访日期：`{meeting_date}`  
参与方：`{participants}`  
录音质量：`{transcript_quality}`

## 0. 开头目录

- 1. 拜访总结摘要
- 2. 业务经验性内容
- 3. 场景收敛清单
- 4. 指标建设路径
- 5. 看板搭建建议
- 6. 推进建议表
- 7. 客户汇报材料
- 8. 下次跟进建议

## 1. 拜访总结摘要

`{summary}`

## 2. 业务经验性内容

| 序号 | 经验类型 | 原文依据 | 提炼结论 | 沉淀价值 | 适用场景 | 置信度 |
|---|---|---|---|---|---|---|
| `{n}` | `{experience_type}` | `{quote}` | `{conclusion}` | `{value}` | `{scene}` | `{confidence}` |

## 3. 场景收敛清单

| 来源 | 场景名称 | 现场反馈 | 优先级 | 可行性 | 关键依据 | 当前阻塞 | 推进建议 |
|---|---|---|---|---|---|---|---|
| `{source}` | `{scene_name}` | `{feedback}` | `{priority}` | `{feasibility}` | `{evidence}` | `{blocker}` | `{next_step}` |

## 4. 指标建设路径

| 场景 | 指标名称 | 计算逻辑 | 数据来源 | 更新频率 | 客户判断逻辑 | 关键约束 | 置信度 |
|---|---|---|---|---|---|---|---|
| `{scene}` | `{metric}` | `{formula}` | `{source}` | `{refresh}` | `{business_logic}` | `{constraint}` | `{confidence}` |

## 5. 看板搭建建议

| 看板主题 | 适用对象 | 使用场景 | 看板目标 | 页面结构建议 | 核心指标 | 故事链 | 建议交付形态 | 是否 Quick Win |
|---|---|---|---|---|---|---|---|---|
| `{board}` | `{audience}` | `{context}` | `{goal}` | `{layout}` | `{metrics}` | `{storyline}` | `{deliverable}` | `{quick_win}` |

## 6. 推进建议表

| 阶段 | 主要做什么 | 解决什么痛点 | 关键实施方案 | 责任人 | 时间节点 | 预期产出 |
|---|---|---|---|---|---|---|
| `{phase}` | `{action}` | `{pain}` | `{plan}` | `{owner}` | `{time}` | `{output}` |

## 7. 客户汇报材料

### 7.1 一句话总结
`{customer_summary}`

### 7.2 推荐场景一页纸

| 场景 | 一句话描述 | 预期价值 | 预计交付时间 |
|---|---|---|---|
| `{scene}` | `{description}` | `{value}` | `{delivery_time}` |

### 7.3 需要客户配合的事项

| 配合项 | 目的 | 负责人 | 时间 |
|---|---|---|---|
| `{item}` | `{purpose}` | `{owner}` | `{time}` |

### 7.4 给 sponsor 的一页价值陈述
`{sponsor_brief}`

## 8. 下次跟进建议

- 下次拜访目标：`{next_goal}`
- 需补信息：`{missing_info}`
- 推荐跟进时机：`{follow_up_timing}`
- 风险预判：`{risk_notes}`
