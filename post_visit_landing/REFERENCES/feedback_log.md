# 使用反馈记录

本文件用于沉淀 CSM、业务团队和平台使用过程中的反馈。每次发现输出质量问题、模型适配问题或交付边界问题，都追加到这里。后续更新 Skill 时，先读取本文件，再决定是否反写到 `SKILL.md`、schema 或其他 `REFERENCES/` 文件。

## 反馈使用规则

- 新反馈优先写成“问题现象 → 影响 → 建议改法 → 状态”
- 若反馈会稳定影响输出质量，应反写到 `SKILL.md` 或对应 schema
- 若反馈只是个案，先留在本文件，不急着改主规则
- 平台如果支持 agent 反写，可让 agent 先读本文件，再更新 Skill 自身

## 当前反馈

| 日期 | 来源 | 问题现象 | 影响 | 已采取动作 | 状态 |
|---|---|---|---|---|---|
| 2026-06-03 | 用户反馈 | 平台级 agent 使用 GLM/DeepSeek 时，长输出广度和深度下滑 | 一次性访后大文档容易漏掉业务经验、场景收敛或推进建议 | 已在 `SKILL.md` 增加弱模型分段 handoff 策略 | 已处理 |
| 2026-06-03 | 用户反馈 | examples 示例可能拉低公司平台模型输出质量 | 弱模型容易模仿示例而不是按现场材料收敛 | 已移除 `examples/` 入口，并从 `SKILL.md` 删除示例引用 | 已处理 |
| 2026-06-03 | 用户反馈 | 公司平台只支持 `SKILL.md`、`SCRIPTS/`、`REFERENCES/` | `assets/`、`README.md`、小写目录可能不被平台识别 | 已把模板迁移到 `REFERENCES/templates/`，脚本迁移到 `SCRIPTS/`，并清理 README/example 入口 | 已处理 |
| 2026-06-03 | 用户反馈 | 需要可持续记录大家使用过程中的反馈 | 反馈散落在聊天里，无法被后续 agent 调用和反写 | 已新增本文件，并在 `SKILL.md` 参考资料中挂载 | 已处理 |

## 待观察

- 客户版简报模板是否会在平台上被稳定识别；若 `REFERENCES/templates/` 不支持二级目录，再把模板文件上移到 `REFERENCES/`
- `prepare_post_visit.py` 是否需要新增“分段 handoff payload”字段，以便弱模型分多轮处理访后材料
