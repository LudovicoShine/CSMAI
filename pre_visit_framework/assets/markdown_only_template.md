# 客户拜访前调研框架

客户：`{customer_name}`  
行业：`{industry}`  
拜访部门：`{visit_target_dept}`  
拜访日期：`{date}`

## 0. 开头目录

- 1. 客户与部门业务情况介绍和推测
- 2. 拜访基本信息摘要
- 3. 总调研框架
- 4. 6 个精准主场景
- 5. 3 个补充贴切场景（仅双线并行时输出）
- 6. 钩子指标雷达
- 7. 推进建议表（预测）
- 8. 场景地图

## 1. 客户与部门业务情况介绍和推测

### 1.1 客户公司业务介绍
`{customer_intro}`

### 1.2 拜访部门业务介绍
`{dept_intro}`

### 1.3 本次拜访判断
`{visit_judgement}`

### 1.4 已知信息
`{known_info}`

### 1.5 合理推测
`{working_hypothesis}`

## 2. 拜访基本信息摘要

`{visit_summary_30_words}`

## 3. 总调研框架

### 3.1 本次调研总目标
`{visit_objective}`

### 3.2 调研方法论
`{methodology}`

### 3.3 六大调研维度
`{research_dimensions}`

### 3.4 完整版现场引导问题框架

| 顺序 | 调研阶段 | 目标 | 具体问题 | 追问方向 | 适用干系人 |
|---|---|---|---|---|---|
| `{n}` | `{stage}` | `{goal}` | `{question}` | `{follow_up}` | `{stakeholders}` |

### 3.5 干系人调研判断提醒
`{stakeholder_notes}`

## 4. 6 个精准主场景

### 场景 S1
| 项目 | 内容 |
|---|---|
| 场景标题 | `{scene_title}` |
| 场景描述 | `{scene_desc}` |
| 面向对象 | `{audience}` |
| 业务动向 | `{rhythm}` |
| 分析思路 | `{analysis_flow}` |
| 分析指标 | `{metrics}` |
| 与产品的相关性 | `{product_relevance}` |
| 场景引导 | `{scene_prompt}` |
| 风险与痛点 | `{risks}` |
| 切入点提示 | `{hooks}` |

## 5. 3 个补充贴切场景

`{optional_additional_scenes}`

## 6. 钩子指标雷达

| 指标名 | 客户说什么词 = 命中 | 业务含义 | 对应主场景编号 |
|---|---|---|---|
| `{hook_metric}` | `{hook_phrase}` | `{hook_meaning}` | `{scene_ref}` |

## 7. 推进建议表（预测）

| 阶段 | 主要做什么 | 解决什么痛点 | 关键实施方案 | 预期产出 |
|---|---|---|---|---|
| `{phase}` | `{action}` | `{pain}` | `{solution}` | `{output}` |

## 8. 场景地图

### 8.1 业务域 × 角色层级地图
`{domain_role_map}`

### 8.2 BI 建设落点图
`{bi_landing_map}`

### 8.3 场景优先级建议
`{priority_map}`
